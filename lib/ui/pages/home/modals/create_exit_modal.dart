import 'package:flutter/material.dart';

class CreateExitModal extends StatelessWidget {
  final String? selectedSpot;
  const CreateExitModal({this.selectedSpot, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
